## 📊 Seaborn

This folder contains **Seaborn-based data visualization and exploratory data analysis (EDA)** work.  
The notebooks focus on understanding relationships, distributions, categories, and correlations using high-level statistical plots.

---

### 📌 Topics Covered
- Relational plots:
  - `relplot()`
  - Use of `hue`, `style`, `row`, and `col`
- Categorical plots (`catplot`):
  - Default **strip plot**
  - `bar`, `box`, `boxen`, `swarm`, and `point` plots
- Distribution plots:
  - `kdeplot()`
- Regression analysis:
  - `regplot()` (scatter + best-fit line)
- Correlation analysis:
  - Correlation matrix using numeric data
  - Heatmap visualization with annotations
  - Diagonal masking using `np.eye()`

---

### 📂 Files
- `basic.ipynb` – Seaborn visualization and EDA practice notebook
